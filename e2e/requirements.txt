# Use pytest for running the tests
pytest==7.4.3
# Use Playwright for controlling browsers during tests
pytest-playwright==0.4.3
# Use Faker for generating test data
Faker==22.2.0
# Use flake8 for checking code quality
flake8==6.1.0
# Use black for code formatting
black==23.12.1
